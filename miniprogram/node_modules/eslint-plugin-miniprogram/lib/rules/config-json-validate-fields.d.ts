/**
 * @fileoverview parsing and linting required and valid JSON config fields.
 */
export declare const configJsonValidateFields: {
    meta: {
        type: string;
        docs: {
            description: string;
            category: string;
            recommended: boolean;
            url: string;
        };
        schema: never[];
    };
    create(context: any): {
        CallExpression(node: any): void;
    };
};
